#include <bits/stdc++.h>
using namespace std;

int printSum(int arr[][4], int row, int col)
{
    for(int i=0; i<row; i++)
    {
        for(int j=0; j<col; j++)
        {
            sum+= 
        }
    }
    return sum;
}

int main()
{
    int r,c=4;
    cin>>r;
    int arr[r][c];
    for(int i=0; i<r; i++)
    {
        for(int j=0; j<c; j++)
        {
            cin>>arr[i][j];
        }
    }

    cout<<printSum(arr, r, c);
}